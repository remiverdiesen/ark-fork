apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: properties-test-agent
spec:
  prompt: |
    You are testing model properties.
    Generate exactly 3 words, then stop.
    Use creative and varied vocabulary.
  modelRef:
    name: test-model-properties
