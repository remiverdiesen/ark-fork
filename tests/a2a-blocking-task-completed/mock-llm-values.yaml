# Mock LLM configuration for A2A blocking task completed test
terminationGracePeriodSeconds: 3
ark:
  a2a:
    enabled: true
    pollInterval: "3s"

config:
  rules:
  - path: "/v1/models"
    method: "GET"
    response:
      status: 200
      content: |
        {
          "object": "list",
          "data": [
            {
              "id": "gpt-4.1-mini",
              "object": "model",
              "owned_by": "openai"
            }
          ]
        }

