apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent
spec:
  prompt: |
    You are an agent for testing input types.
    Respond briefly with "Input type test completed."
  modelRef:
    name: test-model-mock
