apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: model-target-query
spec:
  input: "What is the capital of France? Respond with just the city name."
  target:
    type: model
    name: test-model-mock