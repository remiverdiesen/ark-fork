apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: coordinator
spec:
  modelRef:
    name: test-model-mock
  prompt: "You are the coordinator. If there is a satisfactory final report, repeat it, then use the terminate tool to end the conversation."
  tools:
    - name: terminate
      type: built-in

